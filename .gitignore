# Ignorar RPMs grandes (>100MB são rejeitados pelo GitHub; baixados via script no build)
files/rpms/*.rpm

# Chaves Cosign locais (armazenadas nos Secrets do GitHub)
*.key
keyjoseam
keyjoseam.pub

# Artefatos de build e imagens
*.iso
*.img

